name: usbtethering
description: "Instant USB Tethering Access"
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_launcher_icons: "^0.13.1"

flutter_icons:
  android: true
  ios: false
  image_path: "assets/icon/icon1.png" # ← Match your actual filename

flutter:
  uses-material-design: true
  # NO assets: section needed for launcher icons
